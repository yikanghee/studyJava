package lambda;

public class Lambda {

    // 람다식은 Java 8에 추가된 기술
    // 람다 표현식은 매개변수를 받아 값을 반환하는 짧은 코드 블록
    // 람다 표현식은 메서드와 유사하지만 이름이 필요하지 않으며 메서드 본문에서 바로 구현할 수 있습니다.

    /**
     * Example
     * 가장 간단한 람다 표현식에는 단일 매개변수와 표현식이 포함
     * parameter -> expression
     * parameter1, parameter2 -> expression
     */

    /**
     *
     *
     */


}